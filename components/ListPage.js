import React from "react";
import { StyleSheet, Text, TextInput, View,Switch } from "react-native";
export default function ListPage({itemList,passList}){
    return(
        <View>
            <Text>{taskName}</Text>
            <Switch/>
        </View>
    )
}